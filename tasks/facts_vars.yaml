---
- name: FACTS VARS | Load distribution specific variables
  ansible.builtin.include_vars:
    file: "{{ ansible_distribution | lower | replace(' ', '-') }}-{{ ansible_distribution_major_version }}.yaml"

- name: FACTS VARS | Populate service facts
  ansible.builtin.service_facts:

- name: FACTS VARS | Build package list
  ansible.builtin.package_facts:
